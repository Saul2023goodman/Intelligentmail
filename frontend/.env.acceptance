# Explicit local live-extension acceptance mode. Core still requires an active
# exact Confirmation before it can ask the extension to mutate the mailbox.
SMARTMAIL_ENABLE_EXTENSION_SEND=1
SMARTMAIL_ENABLE_EXTENSION_SCHEDULE=1
