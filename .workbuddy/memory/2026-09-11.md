# 2026-09-11

## Ticket 02: Prepare communications from existing documents (resolved)

Implemented ticket 02 of the SmartMail phase-one effort at the core command/query seam, TDD (red→green per slice). All 34 tests pass with the representative archive (`SMARTMAIL_SAMPLE_ZIP`), no skips.

**Inspected materials** (`sample.zip`, SHA-256 `f2e73a53…98f5e2`): 19 outreach draft `.docx` documents plus one CV. Uniform layout — `Email:` recipient line, `Dear …` greeting, body, `Yours sincerely,` sign-off, trailing `Research source:` internal note. Filename key `<Institution>_<Supervisor name>.docx`.

**Confirmed design decisions with the user before testing:**
- No subject is invented — a missing subject is a blocking readiness finding (no authoritative subject source exists in the materials).
- The trailing `Research source:` line is separated as a supported transformation and kept as an internal note.
- The draft `Email:` line is authoritative for the message recipient; a differing recorded master address is a blocking `recipient_conflict`; the draft fills a missing recorded address.
- Attachment association is deferred to ticket 03.

**Added**: `smartmail/documents.py` (docx paragraph reading, draft parsing, filename association key); schema tables `preparations`, `transformations`, `readiness_findings`, `document_findings`; core methods `prepare_from_documents`, `list_preparations`, `get_preparation`, `preview_preparation`, `list_unassociated_documents`; CLI `prepare`, `preparation list|show|preview`, `imports findings`.

**Representative results**: 17 Preparations, 2 `unassociated_document` (UTS Nahum McLean, Nga Wun Doris Li), 1 `recipient_conflict` (Susanna Castleden), 1 filled missing address (Callum Morton), 17 `missing_subject`; none Ready.

**Notable**: a repeat-preparation bug duplicated document findings — found by the CLI pilot, fixed with a guard + regression test. Recipient comparison is case-insensitive on the full address (real materials have a local-part case-only variant, Robyn Heckenberg) while the declared form is preserved.

**Docs**: `docs/preparation-pattern-02.md`, `docs/ticket-02-validation.md`; README updated; ticket 02 marked resolved with a comment. Pilot store retained at `.smartmail/ticket02-pilot` (gitignored).
